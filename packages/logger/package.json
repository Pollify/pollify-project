{
  "name": "@pollify/logger",
  "version": "1.0.0",
  "private": true,
  "description": "Helper utility for logging",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc -p tsconfig.json",
    "pack": "npm pack"
  },
  "author": "Mike Kotte",
  "license": "ISC",
  "dependencies": {
    "winston": "^3.3.3",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "@types/node": "^14.14.37",
    "typescript": "^4.2.3"
  }
}
